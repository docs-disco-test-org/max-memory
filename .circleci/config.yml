version: 2.1

jobs:
  build-and-test:
    docker:
      - image: node:10-alpine
    resource_class: small
    steps:
      - checkout
      - run: npm install
      - run: npm test
workflows:
    build-and-test:
      jobs:
        - build-and-test
